{
    "git.confirmSync": false,
    "security.workspace.trust.untrustedFiles": "open",
    "redhat.telemetry.enabled": false,
    "files.exclude": {
        "**/*.d.ts": true,
        "**/*.js": {
            "when": "$(basename).ts"
          }
    }
}
